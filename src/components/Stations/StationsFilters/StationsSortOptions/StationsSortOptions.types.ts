import { SetSortOptionSelected, SortOptionSelected } from 'components/Stations/Stations.types';

export interface StationsSortOptionsProps {
  sortOptionSelected: SortOptionSelected;
  setSortOptionSelected: SetSortOptionSelected;
};
